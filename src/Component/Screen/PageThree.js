import React from "react";
import { View, Text } from "react-native-web";

const PageThree = () => (
  <View style={{ flex: 1, justifyContent: "center" }}>
    <Text style={{ textAlign: "center" }}>This is PageThree Screen!</Text>
  </View>
);

export default PageThree;
