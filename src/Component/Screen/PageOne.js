import React from "react";
import { View, Text } from "react-native-web";

const PageOne = () => (
  <View style={{ flex: 1, justifyContent: "center" }}>
    <Text style={{ textAlign: "center" }}>This is PageOne Screen!</Text>
  </View>
);

export default PageOne;
